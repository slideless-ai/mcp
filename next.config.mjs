/** @type {import('next').NextConfig} */
const nextConfig = {
  // The MCP server is an API-only Next app. No image optimization or i18n needed.
  reactStrictMode: true,
};

export default nextConfig;
